# TS Express Microservice (Extended)

Includes:
- Swagger (OpenAPI) docs at `/docs`
- Structured logging (Winston + Morgan)
- Advanced `/health` endpoint with dependency probes

## Run locally

```bash
npm install
cp .env.example .env
npm run dev
```

Then open [http://localhost:3000/docs](http://localhost:3000/docs).
